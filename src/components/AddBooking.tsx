import { useEffect, useState } from "react";

interface IAddBookingProps {
    name: string;
    lastname: string; 
    email: string;
    phone: string;
}

export const AddBooking = () => {

    return( 
        <></>
        // <form>

        //     <input placeholder="Förnamn" value={booking.customer.name} onChange={handleChange} name="customer.name"/>
        //     <input placeholder="Efternamn" value={booking.customer.lastname} onChange={handleChange} name="customer.lastname"/>
        //     <input placeholder="E-post" value={booking.customer.email} onChange={handleChange} name="customer.email"/>
        //     <input placeholder="Telefonnummer" value={booking.customer.phone} onChange={handleChange} name="customer.phone"/>
        //     <div>{JSON.stringify(booking)}</div>
        // </form>
    );
}
