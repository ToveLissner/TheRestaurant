import { createContext } from "react";

export const BookingContext = createContext(false); 

// export const testOfContext = () => {
//     console.log("funka")
//   };

export const testOfContext = false;

  export const TestContext = createContext(testOfContext);